version: '3'

services:
  nodejs-app:
    build: ./nodejs-app
    container_name: nodejs-app
    ports:
      - "3000:3000"
  
  nginx:
    build: ./nginx
    container_name: nginx
    ports:
      - "8080:80"
    depends_on:
      - nodejs-app
